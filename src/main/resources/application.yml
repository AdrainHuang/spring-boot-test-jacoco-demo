spring:
  datasource:
    url: jdbc:h2:mem:testdb  # ??H2?????????????????????
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true  # ??H2????????
  jpa:
    hibernate:
      ddl-auto: update  # ?????????
    show-sql: true  # ??SQL???????

server:
  port: 8085  # ????????8080??
